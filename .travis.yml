language: java
install: mvn install --settings .maven.xml -DskipTests=true -Dgpg.skip -Dmaven.javadoc.skip=true -B -V
jdk:
  - openjdk8
script:
  mvn clean deploy --settings .maven.xml -DskipTests=true -B -U -Prelease
